package com.sanjeewabasnayaka.android.composedapp

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest { // Make sure the name contain the word Test
    @Test
    fun addition_isCorrect() {  // name should be easily understandable
        assertEquals(4, 2 + 2)
    }
    @Test
    fun addition_isWrong() {  // name should be easily understandable
        assertEquals(3, 2 + 2)
    }
}